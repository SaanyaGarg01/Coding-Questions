class Solution {
    public int arraySign(int[] nums) {
       int negcount=0;
       for(int i:nums){
        if(i==0){
            return 0;
        }
        if(i<0){
            negcount++;
        }

       }
               return (negcount%2==0)?1:-1;
    }
}
